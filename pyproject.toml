[tool.poetry]
name = "kkro"
version = "0.1.0"
description = "B language compiler in Python 3"
authors = ["Jahan Addison <jahan@jahanaddison.com>"]

[tool.poetry.dependencies]
python = "3.7.0"
lark = "^0.0.4"
mypy = "^0.650.0"
flake8 = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
